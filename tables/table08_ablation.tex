% Table VIII — Ablation study (pilot campaign; preliminary only).
\begin{table}[!t]
  \caption{Ablation Study on Mixed Campaign (50-node grid, \textsc{Balanced} mode; pilot results).}
  \label{tab:ablation}
  \centering
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{1.08}
  \begin{tabular}{@{}lccc@{}}
    \toprule
    \textbf{Variant} & \textbf{DR\ddag} & \textbf{FPR} & \textbf{$\Delta E$ (\%)$\S$} \\
    \midrule
    Full RPL-ClusterIDS              & 1.06\% & 0.50\% & 7.50\% \\
    Without clustering layer          & 0.33\% & --- & 8.50\% \\
    Without CH two-stage verification & 0.00\% & --- & 8.51\% \\
    Without context adaptation       & 0.04\% & --- & 8.51\% \\
    Without energy awareness         & 0.05\% & --- & 8.51\% \\
    \bottomrule
  \end{tabular}
  \vspace{2pt}
  \raggedright\scriptsize
\ddag{} Per-interval detection rate (fraction of node-intervals with detection; distinct from campaign-level DR marked with \dag{} in other tables) in the pilot campaign.
$\S$ Mean CPU overhead relative to baseline (average of CH and member roles); values are preliminary design-target estimates.
Ablation variants were collected with 3 seeds; FPR per variant is omitted for the pilot study because no false positives were observed in the ablation configurations. These results should be interpreted as exploratory rather than definitive.
\end{table}
