% Table VIII — Ablation study (REAL_RESULT).
\begin{table}[!t]
  \caption{Ablation Study on Mixed Campaign (50-node grid, \textsc{Balanced} mode). Per-interval detection rate.}
  \label{tab:ablation}
  \centering
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{1.08}
  \begin{tabular}{@{}lccc@{}}
    \toprule
    \textbf{Variant} & \textbf{DR\dag} & \textbf{FPR} & \textbf{$\Delta E$ (\%)$\ddag$} \\
    \midrule
    Full RPL-ClusterIDS              & 1.06\% & 0.50\% & 7.50\% \\
    Without clustering               & 0.33\% & --- & 8.50\% \\
    Without CH two-stage verification & 0.00\% & --- & 8.51\% \\
    Without context adaptation       & 0.04\% & --- & 8.51\% \\
    Without energy awareness         & 0.05\% & --- & 8.51\% \\
    \bottomrule
  \end{tabular}
  \vspace{2pt}
  \raggedright\scriptsize
\dag{} Per-interval detection rate (fraction of node-intervals with detection).
$\ddag$ Mean CPU overhead relative to baseline (average of CH and member roles).
Ablation variants collected with 3 seeds; FPR per variant pending full campaign.
Without CH verification, member LAS values never exceed $\tau_{\mathrm{las}} = 0.60$ (no single rule triggers sufficient signal), yielding 0.00\% DR. The non-zero DR of ``Without clustering'' (0.33\%) arises because direct CH--member links still enable two-stage verification without the clustering layer.
\end{table}
